{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "ui-chrome",
  "description": "Permissions for the trusted UI chrome webview",
  "windows": ["main"],
  "webviews": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
